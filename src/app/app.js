'use strict';

/**
 * Main JS file to initialize Backbone router and creates the UI.
 * Router creates the abstract structure, and it in-turn creates the Game View DOM elements.
 *
 * @module app
 */
var Router = require('./router'),
    router = new Router();
